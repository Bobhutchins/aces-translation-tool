{
  "name": "aces-translation-tool",
  "version": "1.0.0",
  "description": "ACES Internal Translation Automation Tool with Claude Sonnet 4 integration",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["translation", "education", "claude", "aces"],
  "author": "ACES",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
